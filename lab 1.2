def decrypt_caesar_brute_force(ciphertext):
    alphabet = string.ascii_lowercase
    results = {}
    
    for shift in range(1, 26):
        decrypted_text = ""
        
        for char in ciphertext:
            if char in alphabet:
                new_index = (alphabet.index(char) - shift) % 26
                decrypted_text += alphabet[new_index]
            else:
                decrypted_text += char

        results[shift] = decrypted_text

    return results


# Given Ciphertext
ciphertext = "mznxpz"

# Perform brute force attack
brute_force_results = decrypt_caesar_brute_force(ciphertext)
brute_force_results
